require 'spec_helper'

describe ActionPrefixer do
  it 'has a version number' do
    expect(ActionPrefixer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
